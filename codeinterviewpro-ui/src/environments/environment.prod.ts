export const environment = {
  production: true,
  // Change this to your real domain (e.g., https://api.yourdomain.com/api) when deploying to the cloud.
  // Using localhost:7000 for local docker-compose testing.
  apiUrl: 'http://localhost:7000/api'
};
